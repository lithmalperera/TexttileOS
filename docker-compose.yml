services:
  postgres:
    image: postgres:18-alpine
    container_name: textile-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-textile}
      POSTGRES_USER: ${POSTGRES_USER:-textile}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-textile-local-only}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-textile} -d ${POSTGRES_DB:-textile}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
