-- Baseline migration for the Textile Manufacturing Management System.
-- Business tables are introduced by feature-owned migrations (IAM-001 onwards).
-- Convention: V<version>__<description>.sql. Applied migrations are immutable.
SELECT 1;
